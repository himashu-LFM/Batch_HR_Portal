{
  "name": "people-lens-combined-batch-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Single AWS Lambda: monthly paid leave credit + daily birthday/anniversary emails",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "zip": "powershell -NoProfile -Command \"if (Test-Path dist.zip) { Remove-Item dist.zip -Force }; Compress-Archive -Path dist\\*,node_modules\\* -DestinationPath dist.zip\"",
    "run:local": "powershell -NoProfile -Command \"$env:RUN_LOCAL='1'; node dist/handler.js\""
  },
  "dependencies": {
    "@aws-sdk/client-sesv2": "^3.967.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.15.5",
    "dotenv": "^16.4.5",
    "typescript": "^5.5.0"
  }
}

